// import Product from "app/core/model/Product"
// import { BlitzApiHandler, BlitzApiRequest, BlitzApiResponse, Middleware, useQuery } from "blitz"

// const handler: Middleware = (req, res, next) => {
//   if (req.method != "GET") next()
//   if (req.query.productId == undefined) next()

//   let id = Number(req.query.productId)
//   let x = Product.random()
//   x.id = id
//   return res.status(200).send(x)
// }

// export default handler

export default 1